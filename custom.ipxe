#!ipxe

# 定义 repo 变量（替换为实际仓库，例如 VzLinux 8 的镜像）
set repo https://repo.virtuozzo.com/vzlinux/8/BaseOS/x86_64/os/

# 加载内核（带参数）
kernel http://ftp.orgv.eu/download.php?file=vmlinuz inst.repo=${repo}

# 加载 initrd（独立命令）
initrd http://ftp.orgv.eu/download.php?file=initrd.img

# 启动引导
boot
